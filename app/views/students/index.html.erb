<div id="new_student">
    <%= render "new_form", student: Student.new %>
</div>
<div id="students">
  <% @students.each do |student| %>
    <%= render "show", student: student %>
  <% end  %>
</div>
